<div class="container">
  <h1>New Review</h1>
    <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit 'Rate!', class: "btn btn-primary" %>
    <% end %>
</div>
